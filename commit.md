- Para atualizar o código utilize o comando:

```sh
git pull
```

- Para commitar o código utilize o comando:

```sh
git add .
git commit -m "MENSAGEM"
```

- Para enviar os commits para o repositório utlize o comando:

```sh
git push
```
